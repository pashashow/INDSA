// app/models/group.js
'use strict';
/*
var mongoose     = require('mongoose');
var Schema       = mongoose.Schema;

//var AgeGroupSchema = require( 'mongoose' ).model( 'AgeGroup' );
//var CategorySchema = require( 'mongoose' ).model( 'Category' );
//var CoupleSchema   = require( 'mongoose' ).model( 'Couple' );

var GroupSchema   = new Schema({
	category : [{ type: Schema.ObjectId, ref: 'Category' }],
	agegroup : [{ type: Schema.ObjectId, ref: 'AgeGroup' }],
	couples  : [{ type: Schema.ObjectId, ref: 'Couple' }]
});

module.exports = mongoose.model('Group', GroupSchema );
*/
